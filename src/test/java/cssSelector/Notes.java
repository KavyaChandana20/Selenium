package cssSelector;

public class Notes
{
    //CSS Selector --> By ID, Class, Element, Attribute
    // Syntax -  tagName[attribute=value]
    //   xpath -->  //tagName[@attribute=value]

    // CSS selectors are used to select elements in an HTML or XML document

    //  Normal Selector  -> Css Selector
    // id -> #id
    // className -> .class
    //  custom Attribute - <input type="text" data-qa="abc"/> --> [data-qa="abc"].
    //  here [data-qa] is the custom attribute
    // we can convert xpath into css Selector also.

    // Css Selectors func
    // For contains() --> * is used
    // For starts-with()  --> ^ is used
    // For end-with()  --> $ is used
}
